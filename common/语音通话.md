如果完全按照你发的那份方案继续，刚刚遇到的问题是：

**`whisper-node` 在 Windows 下编译 `whisper.cpp` 失败。**

具体过程是：

1. `npm install whisper-node` 成功了。
2. `npx whisper-node download` 也成功下载了中文可用的 `small` 模型。
3. 它接着自动执行“编译 whisper.cpp”。
4. 编译脚本不是按 Windows/MSVC 写的，而是按 Linux/macOS 的 GCC 环境写的。

它先缺：

```text
make
```

所以我装了 Build Tools 和 GNU Make。

但补完后，真正的报错变成：

```text
'cc' 不是内部或外部命令
'g++' 不是内部或外部命令
process_begin: CreateProcess(NULL, uname -s, ...) failed.
process_begin: CreateProcess(NULL, which nvcc, ...) failed.
```

意思是这个包的 Makefile 还要求：

```text
cc / g++     GCC 编译器
uname        Unix 系统信息命令
which        Unix 查找命令
-pthread     Unix 线程编译参数
```

而 Windows Visual Studio 提供的是：

```text
cl.exe
```

不是 `cc` 或 `g++`。所以不是你少输了一条命令，而是 **这个 npm 包的 Windows 构建支持不完整/不兼容**。

如果硬要按原方案继续，就得再装一套 MinGW 或 MSYS2，让 Windows 模拟出 GCC + Unix 命令环境；但后面仍可能出现 `pthread`、链接器、Node ABI、Electron 打包等问题。你刚才装 MinGW 的 winget 包也没有成功注册安装，正是这条路线开始变得不稳定的信号。

所以我建议换成官方 `whisper.cpp` 的 Windows 预编译程序：模型还是同一个 `ggml-small.bin`，识别能力不变，只是不用在你的电脑上编译。





你可以把它理解成：**目标没变，模型没变，只是把一个在 Windows 上难以编译的 Node 封装，换成了官方已经编译好的 Windows 程序。**

## 1. 你的原方案为什么不能成功

你的方案是：

```ts
import whisper from 'whisper-node'

whisper(audioPath, {
  modelName: 'small',
  whisperOptions: { language: 'zh' }
})
```

它的流程是：

```text
npm 安装 whisper-node
→ whisper-node 自动编译 whisper.cpp C++ 源码
→ Node.js 直接调用编译后的本地模块
→ 返回文字
```

问题不在中文模型，也不在你的代码。问题发生在第二步：**`whisper-node` 在 Windows 上自动编译失败。**

你实际遇到过的错误依次是：

```text
make 找不到
```

补了 Make 后又变成：

```text
cc 找不到
g++ 找不到
uname 找不到
which 找不到
-pthread 不兼容
```

原因是 `whisper-node` 依赖的构建脚本更偏向 Linux/macOS：

```text
Linux / macOS 常用：make + cc + g++ + uname + which
Windows Visual Studio 提供：cl.exe + cmake
```

它没有正确适配你 Windows 的 MSVC 工具链。即使继续装 MinGW、MSYS2，也可能继续遇到链接、Node 原生模块 ABI、Electron 打包等问题。

所以不是你的思路错了，而是 **`whisper-node` 这个实现方式在 Windows + Electron 环境里不稳定**。

## 2. 现在方案为什么能成功

现在使用的是 [whisper.cpp 官方项目](https://github.com/ggml-org/whisper.cpp?utm_source=chatgpt.com) 的 Windows x64 预编译版。

流程变成：

```text
下载官方已经编译好的 whisper-cli.exe
→ Electron 主进程启动 whisper-cli.exe
→ whisper-cli.exe 加载 ggml-small.bin
→ 返回中文识别文本
```

它不需要你的电脑现场编译 C++，因此不依赖：

```text
make
cc
g++
uname
which
MinGW
MSYS2
```

你已经在 CMD 成功验证过：

```text
6.2 秒中文 WAV
→ Whisper small 模型加载成功
→ 输出中文识别结果
→ 总耗时约 4.5 秒
```

这说明核心语音识别能力已经真实跑通。

## 3. 为什么结果能和原方案一致

因为两套方案底层本质上都是 `whisper.cpp`，并且使用同一个模型文件：

```text
ggml-small.bin
```

两者都使用：

```text
Whisper small 多语言模型
语言参数：zh
输入：WAV 音频
输出：中文文本
```

所以识别结果会基本一致。差别只在调用方式：

| 项目               | 你的原方案                    | 现在方案                        |
| ------------------ | ----------------------------- | ------------------------------- |
| 模型               | `ggml-small.bin`              | `ggml-small.bin`                |
| 中文能力           | 支持                          | 支持                            |
| 离线运行           | 是                            | 是                              |
| API Key            | 不需要                        | 不需要                          |
| 调用方式           | Node 原生绑定                 | 启动 `whisper-cli.exe`          |
| Windows 编译       | 需要                          | 不需要                          |
| 当前电脑是否已验证 | 没有编译成功                  | 已成功识别中文                  |
| 打包稳定性         | 容易受 Electron/Node ABI 影响 | 更稳定，带上 exe、DLL、模型即可 |

最终业务代码仍然可以保持一行：

```ts
const transcript = await transcribeSpeech(audioPath)
```

只是 `transcribeSpeech()` 内部从“调用 Node 原生模块”变成“调用本地 exe”。

## 4. 你需要新增/修改哪些文件

### 新增文件 1：Whisper 服务

路径：

```text
E:\Fairy\Phaethon\ZeroFairyClient\src\main\voice\whisperClient.ts
```

作用：

```text
封装 whisper-cli.exe 的启动、模型路径、中文参数、识别结果解析
```

核心调用：

```ts
spawn(whisperExe, [
  '-m', modelPath,
  '-f', audioPath,
  '-l', 'zh',
  '--no-timestamps'
])
```

------

### 修改文件 2：主进程入口

路径通常就是你刚刚贴的那个文件：

```text
E:\Fairy\Phaethon\ZeroFairyClient\src\main\index.ts
```

新增 import：

```ts
import { transcribeSpeech } from './voice/whisperClient'
```

新增 IPC：

```ts
ipcMain.handle('whisper:transcribe', async (_event, audioPath: string) => {
  try {
    const text = await transcribeSpeech(audioPath)
    return { success: true, text }
  } catch (error) {
    const message = error instanceof Error ? error.message : String(error)
    return { success: false, text: '', error: message }
  }
})
```

作用：

```text
渲染进程 → IPC → 主进程 → whisper-cli.exe → 中文文本 → 渲染进程
```

------

### 修改文件 3：Preload 桥接文件

路径：

```text
E:\Fairy\Phaethon\ZeroFairyClient\src\preload\index.ts
```

在 `api` 对象中新增：

```ts
transcribeSpeech: (
  audioPath: string
): Promise<{ success: boolean; text: string; error?: string }> =>
  ipcRenderer.invoke('whisper:transcribe', audioPath)
```

作用：

```text
让 Vue 渲染进程能安全调用 window.api.transcribeSpeech(...)
```

------

### 新增资源目录 4：Whisper 可执行程序

路径：

```text
E:\Fairy\Phaethon\ZeroFairyClient\resources\whisper\Release\
```

里面放：

```text
whisper-cli.exe
ggml-cpu-alderlake.dll
以及压缩包同目录的其他 DLL
```

------

### 新增资源目录 5：中文模型

路径：

```text
E:\Fairy\Phaethon\ZeroFairyClient\resources\models\
```

里面放：

```text
ggml-small.bin
```

完整结构：

```text
ZeroFairyClient/
├─ resources/
│  ├─ whisper/
│  │  └─ Release/
│  │     ├─ whisper-cli.exe
│  │     └─ *.dll
│  └─ models/
│     └─ ggml-small.bin
│
├─ src/
│  ├─ main/
│  │  ├─ index.ts                 ← 修改
│  │  └─ voice/
│  │     ├─ ttsClient.ts
│  │     └─ whisperClient.ts       ← 新增
│  └─ preload/
│     └─ index.ts                  ← 修改
```

## 5. 当前做到哪一步

你已经完成了最难验证的一步：

```text
whisper-cli.exe + ggml-small.bin + 中文 WAV
→ 成功输出中文
```

现在还没有完成的是：

```text
Vue 录音
→ Blob / WAV
→ 调用 window.api.transcribeSpeech()
→ 拿到文字
→ 自动调用 sendMessage()
```

下一步才是把录音按钮接进来。当前的 `audioPath` 版本可以先用于测试 IPC；正式录音时，接口会改成把录音数据传给主进程，由主进程写入临时 WAV 后识别。